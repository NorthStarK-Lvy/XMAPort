; ============================================
; XMAPort 配置文件
; ============================================

; 设备平台：Qualcomm/MTK，必须如实填写
device_platform=Qualcomm

[source]
; 源 ROM 下载地址 (直接 URL,不能以 ultimateota 开头)
; 用于提取 system / system_ext / product / mi_ext 等分区
url=https://bkt-sgp-miui-ota-update-alisgp.oss-ap-southeast-1.aliyuncs.com/OS3.0.302.4.WOKCNXM/zorn-ota_full-OS3.0.302.4.WOKCNXM-user-16.0-2c191cf91f.zip

[target]
; 底包 ROM 下载地址 (直接 URL,不能以 ultimateota 开头)
; 用于提取 odm / vendor 等目标分区
url=

[settings]
; aria2c 下载设置
; 下载线程数
threads=16

; 最大连接数
max-connection=16

; 超时秒数
timeout=300

; 重试次数
retry=5


[packing]
; ============================================
; 镜像格式等参数设置
; ============================================
format=erofs
; 是否打包生成 super.img
pack_super=true
; 是否生成 sparse 格式镜像
sparse=true
; 分区是否设为只读
readonly=true

; 留空则自动使用 UTC 时间戳，自动判断是否需要
utc_stamp=

; ============================================
; super 分区设置,请按设备填写
; ============================================
; 设备 super 分区的分配总大小,默认 6.5GB
device_size=6979321856

; 其余参数建议保持默认
; metadata 分区大小(字节)
metadata_size=65536

; metadata 插槽数
metadata_slots=3

; 是否启用 Virtual A/B
virtual_ab=true

; super 分区名称
super_name=super

; super 分组名称
super_group=qti_dynamic_partitions

; ============================================
; erofs 压缩选项
; ============================================
; erofs 压缩算法
compression=lz4hc

; erofs 压缩等级
compression_level=8

; 是否兼容旧内核(-E legacy-compress)
erofs_old_kernel=false

; ============================================
; ext4 选项(format=ext4 时生效)
; ============================================
; ext4 打包工具：make_ext4fs 或 mke2fs
ext4_packer=make_ext4fs

; ============================================
; 这一部分建议默认
; ============================================
; false 时,system_ext 将使用 tools/erofs-utils-cygwin 中的旧版 mkfs.erofs 打包
; 当 source_filesystem/product/etc/build.prop 中 ro.product.build.version.incremental 同时包含 V13 和 DEV 时,erofs 打包将自动使用旧版工具
is_skip_apex=false

; 是否注入 adb debug
enable_adb_debug=true

; 是否禁用 vbmeta 校验
patch_vbmeta=true

; ============================================
;  Build Prop Patch List，可以自行添加prop，如自行添加不保证开机
; ============================================
; patch build prop list
####################################
# from device/xiaomi/mivendor/common_rw.prop
####################################
# For common rw override property
#support night charge property
persist.vendor.night.charge=true
####################################
# from device/xiaomi/zero/odm/special_rw.prop
####################################
#support accelerate charge property
persist.vendor.accelerate.charge=true
#support battery health
persist.vendor.battery.health=true
#enable extreme battery
persist.vendor.extreme.mode=true
#support high temp dialog
persist.vendor.hightemp.notice=true
#support smart charge engine
persist.vendor.smartchg=6
#camera wechat beauty
persist.vendor.vcb.ability=true
persist.vendor.vcb.enable=false
ro.vendor.cabc.enable=fasle
ro.vendor.bcbc.enable=true
ro.vendor.dfps.enable=false
ro.vendor.fps.switch.default=true
ro.vendor.gcp.enable=1
ro.vendor.display.default_fps=90
ro.vendor.sre.enable=true
ro.vendor.localhbm.enable=true
ro.vendor.video_box.version=2
